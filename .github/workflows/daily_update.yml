name: Daily Weather Update 🌦️

on:
  schedule:
    - cron: '30 3 * * *'  # रोज सकाळी 9 वाजता (IST)
  workflow_dispatch:      # मॅन्युअली रन करण्यासाठी बटण

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pandas requests pyyaml openpyxl streamlit

      - name: Run Data Pipeline
        run: python main.py

      - name: Commit and Push Data
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          git add data/weather_data.db
          git commit -m "🌦️ Auto-updated weather data" || echo "No changes to commit"
          git push